import 'package:flutter/cupertino.dart';

class requestedReports extends StatefulWidget {
  const requestedReports({Key? key}) : super(key: key);

  @override
  State<requestedReports> createState() => _requestedReportsState();
}

class _requestedReportsState extends State<requestedReports> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
